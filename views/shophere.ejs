<!DOCTYPE html>
<html lang="en">
    <head>
        <!-- default -->
            <meta charset="UTF-8">
            <meta http-equiv="X-UA-Compatible" content="IE=edge">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <!-- Icons -->
            <link rel="icon" href="./img/favicon.ico">
            <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
            <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
            <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" />
        <!-- CSS-JS -->
            <link rel="stylesheet" href="new.css">
        
        
        <!-- Fonts -->
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=League+Spartan:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
        
        <!-- Bootstrap -->
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <style>
            .offcanvas{
                background-color: #79ee3f;
            }
            .offcanvas-body{
                background-color: #E5F9DB;
            }

            .la{
                background-color: #83b768;
                border-radius: 10%;
                margin-bottom: 5px;
            }
        </style>
            <title>OneStopShop</title>
        </head>


<body style="background-color: #E5F9DB">
    <!-- Offcanvas Sidebar -->
    <div id="hidden" hidden><%-JSON.stringify(data)%></div>
<div class="offcanvas offcanvas-start" id="demo">
    <div class="offcanvas-header">
      <h1 class="offcanvas-title">Your Cart</h1>
      <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas">X</button>
    </div>
    <div class="offcanvas-body">
      <div id="cart-values">Nothing In Cart</div>
      <div id="total-cart">Total</div>
      </div>
  </div>
  




    
    <!-- NAVBAR -->
    <header>
        <nav class="navbar navbar-expand-sm sticky-top" id="navbar">
            <div class="container-fluid">
                <a class="navbar-brand" href="#"><img src="img\OneStopShop.png" alt="Logo" id="logo"></a>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mynavbar">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="mynavbar">
                    <div>
                        <ul class="navbar-nav me-auto">
                            <li class="nav-item">
                                <a class="nav-link" href="/shophere">Shop</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/shopkeeper">Shopkeeper</a>
                            </li>
                        </ul>
                    </div>
                    <div>
                        <form class="d-flex" action="/" method="post">
                            <input class="form-control me-2" type="text" name="search" placeholder="Search">
                            <button class="btn btn-primary" type="submit" name="submit-btn">Search</button>
                        </form>
                    </div>
                </div>
            </div>
        </nav>
    </header>

      <!-- Button to open the offcanvas sidebar -->


  <button class="btn btn-primary" type="button" data-bs-toggle="offcanvas" data-bs-target="#demo" style="position:fixed;top:30vh;right:0">
    View Cart
  </button>
  <button style="position:fixed;top:40vh;right:0" class="btn btn-secondary" type="button" id="checkOut">Checkout</button>



<!-- Grid for Shopping items -->
  <div class="container px-4 py-5" id="icon-grid">
    <h2 class="pb-2 border-bottom">Reviews</h2>

    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4 py-5" id="features-append">





      <!-- ============================ -->
      <% data.forEach(function(element) { %>
        <div class="col d-flex align-items-start">
        <img src="<%=element.imageData.slice(1)%>" class="bi text-body-secondary flex-shrink-0 me-3" width="1.75em" height="1.75em">
        <div>
          <h3 class="fw-bold mb-0 fs-4"><%=element.itemName%></h3>
          <div class="row">
            <span class="col"><p>Available-Quantity</p><%=element.itemQuantity%></span>
            <span class="col"><p>Price</p><%=element.itemPrice%></span>
          </div>
          <div class="row">
            <button class="add-remove" id = <%= "-"+element._id %>>-</button>
            <span class="col" id=<%=element._id%>><p>0</p></span>
            <button class="add-remove" id = <%= "+"+element._id %>>+</button>
            
            
          </div>
         
          <p><%=element.description%></p>
        </div>
      </div>
      <% }); %>



    </div>
  </div>

  <% var data = data %>
<!-- Shopping Album -->
<!-- album -->

<!-- footer -->
<div class="container">
  <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
    <p class="col-md-4 mb-0 text-body-secondary">© 2023 Company, Inc</p>

    <a href="/" class="col-md-4 d-flex align-items-center justify-content-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none">
      <svg class="bi me-2" width="40" height="32"><use xlink:href="#bootstrap"></use></svg>
    </a>

    <ul class="nav col-md-4 justify-content-end">
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Home</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Features</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">Pricing</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">FAQs</a></li>
      <li class="nav-item"><a href="#" class="nav-link px-2 text-body-secondary">About</a></li>
    </ul>
  </footer>
</div>

  <!-- Js Files  -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
<script >
var myData = JSON.parse(document.getElementById("hidden").textContent);

  console.log(myData);


var arrayOfItems={}; 

const buttons = document.querySelectorAll('.add-remove');

// Loop through the buttons and add a click event listener to each
buttons.forEach((button) => {
    
  button.addEventListener('click', () => {
    // Get the id of the button that was clicked
    const buttonId = button.id;
    var quantity=parseInt(document.getElementById(buttonId.slice(1)).textContent);
    
    if(buttonId[0]=="+"){
        var add = document.getElementById(buttonId.slice(1));
        quantity = parseInt(add.textContent);
        quantity++;
        add.textContent = quantity;
    }

    if(buttonId[0]=="-"){
        var subtract = document.getElementById(buttonId.slice(1));
        quantity = parseInt(subtract.textContent);
        if(quantity>0){
        quantity--;}
        subtract.textContent = quantity;
    }
    var total = 0;
    arrayOfItems[buttonId.slice(1)] = [quantity];
    document.getElementById("cart-values").innerHTML="";
    for (const key in arrayOfItems){
        console.log(typeof(key));
        myData.forEach((element)=>{
            console.log(typeof(element["_id"]))
        if (key==element._id){
            document.getElementById("cart-values").innerHTML+=`<div class="row"><div class="col la">${element.itemName}</div><div class="col la">${arrayOfItems[key][0]}</div><div class="col la">${element.itemPrice}</div><div class="col la">${parseInt(element.itemPrice.slice(1))*arrayOfItems[key][0]}</div></div>`;
            arrayOfItems[key].push(element.itemName);
            arrayOfItems[key].push(element.itemPrice);
            total+=parseInt(element.itemPrice.slice(1))*arrayOfItems[key][0]
        }
    })
    }
    document.getElementById("total-cart").innerhtml = `<strong>${total}</strong>`;

    
// Or do whatever you want with the buttonId
  });
});

const button = document.getElementById("checkOut");

button.addEventListener("click", () => {
  const data = arrayOfItems;

  const options = {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify(data)
  };

  fetch("/shophere", options)
    .then(response => {
        window.location.href ="/checkout"
      return response.json();
    })
    .then(data => {
      console.log(data);
      alert("If you are not redirected automatically, <a href='/checkout'> click here</a>.")
    })
});


</script>
</body>
</html>